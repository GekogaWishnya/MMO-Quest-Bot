# 🤖 MMOQuest Bot

Автоматический бот для игры [MMOQuest](https://mmoquest.com), работающий в Docker.

## ✨ Особенности

- ✅ Автоматический фарм мобов
- ✅ Работает 24/7 в фоновом режиме
- ✅ Автоматический перезапуск при сбоях
- ✅ Не требует установки Python, Firefox и других зависимостей
- ✅ **Поддержка нескольких ботов одновременно** (мультиаккаунт)

## 📋 Требования

Единственное требование - установленный **Docker**:

  ```bash
  sudo apt-get update && sudo apt-get install -y docker.io docker-compose
  ```


## 🚀 Быстрый старт (один бот)

### Шаг 1: Скачайте проект

```bash
git clone https://github.com/pulsarbf/mmo-quest-bot.git
cd mmo-quest-bot
```

Или скачайте ZIP.

### Шаг 2: Измените файл с настройками

Откройте файл `.env` любым текстовым редактором и заполните:

```env
LOGIN=ваш_логин
PASSWORD=ваш_пароль
MOB=Чумовой гриб
TIMEOUT=30
```

⚠️ **Важно**: Замените `ваш_логин` и `ваш_пароль` на реальные данные от игры!

### Шаг 3: Запустите бота

```bash
docker-compose up -d
```

**Готово!** 🎉 

При первом запуске Docker скачает образ бота, это займет некоторое время.


## 👥 Мультиаккаунт (несколько ботов одновременно)


### 🚀 Быстрый старт для мультиаккаунта

```bash
# Сделайте скрипты исполняемыми (один раз)
chmod +x *.sh

# Добавьте ботов
./add-bot.sh Player_123 mypass123 "Чумовой гриб"
./add-bot.sh Player_456 mypass456 "Бешеный репий" 60
./add-bot.sh Player_789 mypass789 "Грак"

# Запустите всех
./start-all.sh

# Проверьте логи
./logs.sh
```

### 📊 Управление несколькими ботами

| Действие | Команда |
|----------|-----------|
| Добавить бота | `./add-bot.sh <LOGIN> <PASS> <MOB>` |
| Запустить всех | `./start-all.sh` |
| Запустить конкретных | `./start-bots.sh <LOGIN1> <LOGIN2>` |
| Остановить всех | `./stop-all.sh` | `.\stop-all.ps1` |
| Остановить конкретных | `./stop-bots.sh <LOGIN1> <LOGIN2>` |
| Логи всех ботов | `./logs.sh` |
| Удалить бота | `./remove-bot.sh <LOGIN>` |

### 💡 Примеры

**Запустить только двух конкретных ботов:**
```bash
./start-bots.sh Player_123 Player_456
```

**Остановить одного бота:**
```bash
./stop-bots.sh Player_789
```

**Посмотреть логи конкретного бота:**
```bash
cd bots/bot-Player_123
docker-compose logs -f
```


## 📊 Управление одним ботом

### Посмотреть что делает бот (логи)

```bash
docker-compose logs -f
```

Нажмите `Ctrl+C` чтобы выйти (бот продолжит работать).

### Остановить бота

```bash
docker-compose down
```

### Перезапустить бота

```bash
docker-compose restart
```

### Изменить настройки (логин, пароль, моб)

1. Откройте файл `.env`
2. Измените нужные параметры
3. Перезапустите: `docker-compose restart`

**Пересборка не требуется!**

### Обновить бота до новой версии

```bash
docker-compose pull
docker-compose up -d
```

### Проверить, работает ли бот

```bash
docker ps
```

Вы должны увидеть контейнер `mmo-quest-bot` со статусом `Up`.


## 🔧 Настройки

Все настройки находятся в файле `.env`:

| Параметр | Описание | Обязательно |
|----------|----------|-------------|
| `LOGIN` | Логин от аккаунта MMOQuest | ✅ Да |
| `PASSWORD` | Пароль от аккаунта | ✅ Да |
| `MOB` | Имя моба для атаки (на русском) | ✅ Да |
| `TIMEOUT` | Таймаут перезагрузки (секунды) | ❌ Нет (по умолчанию 30) |

**Примеры имён мобов:**
- Чумовой гриб
- Бещеный репий
- Кабан
- Грак


## 🔢 Коды ошибок


| Код | Тип ошибки | Что делает Docker |
|-----|------------|-------------------|
| **0** | Успешное завершение | ✅ Не перезапускается |
| **78** | Ошибка конфигурации (.env не заполнен) | ⛔ Не перезапускается |
| **1** | Временная ошибка (сеть, сайт недоступен) | 🔄 Перезапускается автоматически (до 5 раз) |

**Это означает:**
- Если вы забыли заполнить `.env` - бот остановится с понятной ошибкой и НЕ будет бесконечно перезапускаться
- Если сайт временно недоступен - Docker автоматически перезапустит бота
- После 5 неудачных попыток перезапуска - бот остановится


## ⚠️ Disclaimer

Этот бот создан в образовательных целях.

**Используйте на свой страх и риск.**

Авторы не несут ответственности за:
- Блокировку аккаунта
- Потерю игрового прогресса
- Любые другие последствия использования


## 📄 Лицензия

MIT License - используйте свободно, на свой риск.
